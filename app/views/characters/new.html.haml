%h1 Characters controller, new action


= error_messages_for @character

= form_for(@character, :action => url(:characters) ) do
  %p
    = text_field :name, :label => "Name"  
  %p
    = submit "Create"

= link_to 'Back', url(:characters)